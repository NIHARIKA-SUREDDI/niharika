// Provide an example of using the `forEach` method on an array.
//The forEach() method calls a function for each element in an array.
let numbers = [1, 2, 3, 4, 5];
numbers.forEach(function(number) 
{
    console.log(number);
});


